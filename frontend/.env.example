# URL base del backend. En dev podés usar:
# - http://localhost:8080/api (CORS, habilitado en backend)
# - /api (usa el proxy de Vite configurado en vite.config.js)
VITE_API_URL=http://localhost:8080/api

